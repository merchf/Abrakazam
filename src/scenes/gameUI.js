
export default class GameUI extends Phaser.Scene {
/*
  constructor(scene) {
    super({ key: 'gameUI' });
   this.hud = scene.add.sprite(10,100, 'Life3');
    this.hud.setDepth(20);
    t1his.hud.setTexture('Life3');
  }

  updateLife(health) {
    if (health <= 0) {
      this.hud.setTexture('Life4');
    }
    else if (health === 1) {
      this.hud.setTexture('Life3')
    }
    else if (health === 2) {
      this.hud.setTexture('Life2')
    }
    else if (health === 3) {
      this.hud.setTexture('Life1')
    }
  } */
}